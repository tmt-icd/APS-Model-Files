subsystem=APS
component=ICS.PSH.Detector
subscribe {
    description="""HCD CurrentState subscriptions."""
    

    currentStates = [
        {
            name = "currentState"
            subsystem = APS
            component = ICS.HCD.Detector
        }
    ]
}
