subsystem=APS
component=ICS.APT.Detector
subscribe {
    description="""HCD CurrentState subscriptions."""
    

    currentStates = [
        {
            name = "currentState"
            subsystem = APS
            component = ICS.HCD.Detector
        }
    ]
}
